<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.HeheJuice.Notes">
    <!-- Allows the app to make network calls -->
<uses-permission android:name="android.permission.INTERNET" />
<!-- Allows checking if Wi-Fi or Cellular is actively connected -->
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_hehe"
        android:label="@string/app_name"
        android:theme="@style/Theme.NoteApp"
        android:supportsRtl="true">

        <activity
            android:name=".NotesMainAct"
            android:exported="true"
            android:theme="@style/Theme.NoteApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Add this line -->
        <activity android:name=".NoteEditorActivity" />
        <activity android:name=".NotesUpdate" />

    </application>

</manifest>